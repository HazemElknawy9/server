@extends('layouts.app')

@section('content')
<div class="container">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header center">{{ __('Verification Account') }}</div>

                <div class="card-body">
                    @if (session('resent'))
                        <div class="alert alert-success" role="alert">
                            {{ __('A fresh verification link has been sent to your email address.') }}
                        </div>
                    @endif

                    <p>Thank you For Registration We Send Verify Link To <strong>{{ auth()->user()->email }}</strong></p>
                    <p>Please Go To Your Email Inbox And Verify Your Email Not Your Email </p>
                    <p>Not Your Email <a href="{{ url('/register') }}">{{ __('click here Changed') }}</a>. </p>
                    {{ __('If you did not receive the email') }}, <a href="{{ route('verification.resend') }}">{{ __('click here to request another') }}</a>.
                </div>
            </div>
        </div>
    </div>
</div>
@endsection
